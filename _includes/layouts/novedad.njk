{% extends "src/base.njk" %}

{% block head %}
<title>Noticias | {{ title }}</title>

{% endblock %}

{% block content %}
    

<!--POST-->
<!-- Content
============================================= -->
<section id="content px-4">
  <div class="content-wrap">

    <div class="row">
      <div class="col-xl-12 col-lg-12 col-md-12">
        <article class="container">
         
          <section class="content text-justify">
            <div class="row mt-40 mb-40">
              <div class="col-lg-12">
                <img src="{{image}}" class="w-100">
              </div>
              
              <div class="col-xl-8 col-lg-8 col-md-8 col-sm-12 px-4 h2-margin">

                <h1 style="font-size:45px">{{ title }}</h1>

                <h6 class=" font-bold text-1xl text-gray-800">
                  {{ description }}
                </h6>

                <div class=" text-sm text-page mb-40">
                  Autor:
                    <span class="text-sm font-semibold text-gray-900 ml-2">
                      <!--autor correo fecha-->
                          {{ autor }} | {{ correo }} | {{ date|dateFormat('DD/MM/YYYY')}}
                    </span>
                </div>
                <div class="content-blog">
                  {{ content | safe}}
                </div>
              </div>
              <div class="col-xl-4 col-lg-4 col-md-4 col-sm-12 px-4" >
                <h4 class="pt-4">MÁS BLOGS</h4>
                <div class="row">
                {% for post in collections.novedades | reverse | limit(4)%}
                {% if page.url !== post.url %}
                  <div class="col-lg-12">
                    <a href="{{ post.url }}" class="border-b-2 p-4 mb-4 text-current">
                        <img src="{{ post.data.image }}" alt="Novedad" class="w-100">
                        <div class="my-auto">
                        <h5 class="font-bold text-lg">
                            {{ post.data.title }}
                        </h5>
                        <span class="text-sm">
                            {{ post.data.description }} 
                        </span>
                        </div>
                    </a>
                  </div>

                {% endif %}
                {% endfor %}
                </div>
              </div>  
            </div>

          </section>
          
        </article>
      </div>

    </div>
  </div>
</section>
{% endblock %}